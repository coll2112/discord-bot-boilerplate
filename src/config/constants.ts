export const MESSAGES = {
  commandError: "Something went wrong while running that command.",
  guildOnly: "This command can only be used in a server.",
  unknownCommand: "I do not know how to run that command."
} as const;
